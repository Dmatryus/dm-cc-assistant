# v0.3.0 — Design Notes

Working document. Captures architectural decisions made during design discussion.
Subject to refinement until v0.3.0 is implemented and released.

---

## 1. Концепция

«Долго думаем → автономно делаем → структурированный отчёт → диалог».

Задачи становятся крупнее (эпики), агент сам запускает параллельные имплементации в worktree'ах. Точки взаимодействия с пользователем — до старта (планирование) и после (отчёт).

---

## 2. Внешний интерфейс — 4 команды

**Команды без аргументов.** Активный эпик определяется автоматически из `## In Progress` секции `.task/backlog.md`. По правилу «один эпик за раз» он всегда один.

| Команда | Что делает | Интерактивность | Pre-condition |
|---|---|---|---|
| `/backlog` | Генерирует / показывает эпики, sync с docs | Лёгкий диалог | — |
| `/plan` | Декомпозиция активного эпика на подзадачи, DAG, волны, риски, DoD | Тяжёлый интерактив | Один эпик в In Progress |
| `/execute` | Параллельный прогон подзадач активного эпика + диалог по отчёту | Автономно + диалог в конце | In Progress эпик + `plan-E-XXX.md` |
| `/release` | Подготовка release-материалов для активного эпика | Подтверждения | In Progress эпик + `report-E-XXX.md` |

**Транзишены статуса:**
- Todo → In Progress: внутри `/backlog` (action `pick from Todo`)
- In Progress → Done: внутри `/release` (после commit'а release prep)
- In Progress → Cancelled: внутри финального диалога `/execute` (action abort epic)

Если pre-condition не выполнен — команда **входит в recovery-диалог** с вариантами восстановления и опциональным chain'ом к другой команде (см. §13.7). Пример: `/plan` без активного эпика → «Активного эпика нет. Открыть Todo и выбрать сейчас?» → если yes → запуск branch'а `/backlog` (pick from Todo) → возврат к `/plan` → продолжение. Команда **не отказывает**, она помогает дойти до нужного состояния.

Старые `/research`, `/review`, `/update-docs` снаружи **исчезают**. Соответствующие skills удаляются.

---

## 3. Агенты

**Internal engine.** Эти агенты не вызываются пользователем напрямую — orchestrator-команды (`/backlog`, `/plan`, `/execute`, `/release`) спаунят их через Task tool. Modes / контексты — внутренняя кухня, видимая только в этом разделе.

| Агент | Статус в v0.3 | Роль |
|---|---|---|
| `backlog-planner` | Обновить | Генерит эпики, sync, suggestions, миграция |
| `epic-planner` | **Новый** | Тяжёлое планирование одного эпика |
| `execution-agent` | **Новый** | Автономная работа в worktree |
| `resume-analyzer` | **Новый** | Анализ прерванной подзадачи для построения `.task/resume-E-XXX.X.md` |
| `release-manager` | Расширить — **три mode** | `merge` (между волнами в /execute) + `aggregation` (после волн в /execute) + `full` (release prep в /release) |
| `code-reviewer` | Расширить — **два mode** | `in-execute` (review после волн, code quality) + `release-readiness` (pre-release review, shipping readiness) |
| `docs-updater` | Внутренний | Targeted edits docs с `[Priority]` поддержкой |
| `task-researcher` | Удалить | Работа поглощена `/plan` |

---

## 4. Backlog — модель данных

### ID-схема
- Эпик: `E-001`
- Подзадача: `E-001.1` (точечная нотация — родитель виден из ID)

### Статусы

| Уровень | Значения | Где хранится |
|---|---|---|
| Эпик | `Todo` / `In Progress` / `Done` / `Cancelled` | Через секцию в `backlog.md` (включая отдельную `## Cancelled`) |
| Подзадача | `Todo` / `In Progress` / `Done` / `Failed` / `Skipped` / `Conflict-blocked` / `Cancelled` | Inline в строке подзадачи |

**Семантика статусов подзадачи:**
- `Failed` — execution-agent упёрся в obstacle
- `Skipped` — cascade: зависимость не Done
- `Conflict-blocked` — high-stakes конфликт, требует пользовательского решения в финальном диалоге (см. §13.5)
- `Cancelled` — пользователь явно отказался: «Skip permanently» в финальном диалоге, либо `[k] Skip merge` для conflict'а

### Тип эпика (опциональный inline-лейбл)
- `Functional` (default, можно опустить)
- `Tech Debt`
- `Research`
- `Infrastructure`
- `Cleanup`

### Параллелизм
- Внутри эпика: волны подзадач (параллельные worktree'ы)
- Между эпиками: **один эпик In Progress за раз** (линейный pipeline)

### Размер эпика
**Variant C (закреплён):** эпик = логическая группа задач с самостоятельной ценностью. Размер плавающий, определяет пользователь. Нет жёсткой привязки к minor-релизу.

---

## 5. Артефакты

### State files (frequently updated)

| Файл | Создаётся / обновляется | Содержит |
|---|---|---|
| `.task/backlog.md` | `/backlog`, `/plan` (добавляет подзадачи), `/execute` (статусы), `/release` (Done/Cancelled) | Эпики + подзадачи + статусы + Parking lot + Rejected suggestions |
| `.task/backlog-archive.md` | `/release` (auto-archive после Done > 5) | Старшие закрытые эпики (Done и Cancelled) |

### Per-epic artifacts (per active epic)

| Файл | Создаётся | Содержит |
|---|---|---|
| `.task/plan-E-XXX.md` | `/plan` accept | Контекст, подзадачи, DAG, волны, риски, альтернативы, open questions, DoD |
| `.task/report-E-XXX.md` | `/execute` aggregation step | Aggregated report — Summary / Wave breakdown / Obstacles / Conflicts / Review / Open questions / Decisions / Follow-ups / Integration state |

### Per-subtask artifacts (per subtask in active epic)

| Файл | Создаётся | Содержит |
|---|---|---|
| `.task/report-E-XXX.X.md` | execution-agent перед return'ом | 5 секций: Done / Decisions / Obstacles / Open questions / Follow-ups (+ header с Status, Branch, Commits, Smoke check) |
| `.task/research-E-XXX.X.md` | execution-agent типа `research` | Question / Options / Decision / Rationale / Implications. Мёрджится в feat/E-XXX |
| `.task/resume-E-XXX.X.md` | resume-analyzer (при выборе Resume в pre-execute analysis) | Что сделано / что осталось / risky parts / decisions из прошлого / контекст / recommendation |

### Release artifacts (per release)

| Файл | Создаётся | Содержит |
|---|---|---|
| `.task/release-notes-vX.Y.Z.md` | `/release` step 5 | Пользовательский язык, highlight'ы, install/upgrade — для GitHub Release |
| `.task/announcement-vX.Y.Z.md` | `/release` step 5 | Короткий цепляющий текст — для TG / блога / social |
| `.task/migration-vX.Y.Z.md` | `/release` step 5 (только при breaking) | Шаги обновления для пользователей |

### Operational / transient

| Файл | Создаётся | Содержит |
|---|---|---|
| `.task/.execute-active` | Orchestrator при старте `/execute`, удаляется при завершении | Marker file — индикатор активного `/execute` для SessionStart hook'а и предупреждений |

**Edit log convention** (см. §12.3) применяется ко всем генерируемым файлам выше, кроме `.execute-active` (machine-only marker).

---

## 6. Модель веток

```
main
 └─ feat/E-001                      (создаёт /execute)
     ├─ feat/E-001.1 worktree       (волна 1)
     ├─ feat/E-001.2 worktree       (волна 1)
     └─ feat/E-001.3 worktree       (волна 2, после merge волны 1)
```

### Жизненный цикл веток
1. `/execute` создаёт `feat/E-001` от main.
2. Execution-agent владеет своим worktree.
3. Между волнами `release-manager` мёрджит подзадачи в `feat/E-001`.
4. Следующая волна стартует с обновлённого `feat/E-001`.
5. `/release` финально мёрджит в main.

---

## 7. Поведение при преграде (cascade по DAG)

1. Подзадача упёрлась в непреодолимое → останавливается, пишет в свой отчёт.
2. Параллельные в текущей волне — добегают.
3. В следующих волнах: зависимые от упавшей → `Skipped`, независимые → запускаются.
4. `/execute` финиширует в `partial`, диалог с пользователем по агрегированному отчёту.

---

## 8. `/backlog` — детальный дизайн

### Источники для генерации эпиков

| Источник | Тип эпика | Дефолт. приоритет |
|---|---|---|
| OVERVIEW §6 Must | Functional | Critical / High |
| OVERVIEW §6 Should | Functional | Medium |
| OVERVIEW §6 Could | Functional | Low |
| OVERVIEW §9 Open Questions (блокирующие Must) | Research | Critical / High |
| ARCHITECTURE §9 Tech Debt | Technical | По `[Priority]` тегу |
| ARCHITECTURE §10 Code Hotspots | Technical | По `[Priority]` тегу |
| ARCHITECTURE §8 Constraints (требующие setup) | Infrastructure | High |
| Gap «доки vs реальный код» | Cleanup / migration | Medium |
| OVERVIEW §6 Won't + §7 Non-goals | — | Игнорируются явно |

### Объём draft
5–12 эпиков. Если получается больше — слишком мелкая нарезка, агент предупреждает.

### Сценарии запуска

#### Первый запуск (пустой backlog)
1. Read OVERVIEW + ARCHITECTURE + light code scan.
2. Если хоть одного из docs нет — **отказ**, отправка к `/dm-cc-assistant:project-init`. Никаких degraded mode.
3. Generate draft эпиков (строго из docs/code-gap, ничего «от себя»).
4. Discuss draft с пользователем (split / merge / rename / reprioritize).
5. **Suggestions phase** (опционально) — кандидаты с обоснованием.
6. Записать `.task/backlog.md`.

#### Повторный запуск (backlog есть)
1. Read existing backlog.md.
2. **Sync check:** lightweight diff между «что было бы в backlog из текущих docs» и фактическим состоянием.
3. Surface deltas → пользователь решает по каждому (см. ниже).
4. Stand. меню: `advance` / `pick from Todo` / **`add new epic`** / `quit`.

#### Миграция с v0.2 (плоский T-ID формат)
- Detect плоский формат → спрос: **migrate** (default) / **wipe** (с бэкапом в `.task/backlog.v02.md`) / **keep-legacy** (старые в секции `## Legacy`, новые эпики в Todo).

### Suggestions — что от себя предлагает агент

**Запрет:** автономно добавлять в draft.
**Разрешено:** после draft'а — отдельная фаза с обоснованием.

**Фильтр:** только сильное обоснование (видимый признак риска), не «best practice вообще».

**Три состояния:**

| Действие | Куда попадает | Будет ли предложено снова |
|---|---|---|
| Принять | Эпик в `## Todo` | — |
| Отложить | `## Parking lot` секция | Нет, лежит на виду |
| Отклонить | `## Rejected suggestions` | Нет, пока пользователь не удалит строку |

**Две линии защиты от спама:**
1. OVERVIEW §7 Non-goals (явный proactive фильтр)
2. `## Rejected suggestions` (накопительный reactive фильтр)

**Без жёсткого capa** — оставляем только фильтр на качество обоснования.

### Add new epic — ручное добавление

Запускается из меню повторного запуска `/backlog`. Не отдельный command.

1. Пользователь даёт намерение (title + 1–2 фразы).
2. **Агент читает OVERVIEW + ARCHITECTURE + текущий backlog** (light scan).
3. **Validate step** — проверка на 4 признака сомнения:
   - Конфликт с Won't / Non-goals
   - Дубликат / overlap с существующим эпиком
   - Нет источника в docs (можно, но агент уточняет)
   - Подразумеваемая зависимость от существующего эпика
4. **Если сомнений нет** → fast path с короткими уточняющими вопросами.
5. **Если есть** → диалог по каждому concern, пользователь решает.
6. Не блокирующий: «я в курсе, добавляй» проходит без дальнейших вопросов.
7. Запись в выбранную секцию (`Todo` дефолт, `Parking lot` при желании).

### Sync check — детект изменений в docs

Запускается автоматически в начале `/backlog` (когда backlog уже есть). Lightweight, не блокирующий.

**Типы deltas:**

| Что произошло в docs | Что предлагает агент |
|---|---|
| Новый Must / Should / Could / Tech Debt / Hotspot | Добавить новый эпик |
| Удалён пункт, на который ссылался эпик | Deprecate / keep / Parking |
| Изменился `[Priority]` ([Medium] → [High]) | Обновить приоритет эпика |
| Изменена формулировка / контекст | Обновить контекст эпика? (опционально) |

**Sync НЕ:**
- Не трогает In Progress / Done эпики автоматически
- Не пересоздаёт с нуля
- Не удаляет parking / rejected
- Если deltas нет — молча идёт в меню («docs and backlog in sync»)

**Edge case:** если deltas очень много (OVERVIEW переписан с нуля) → один большой выбор: sync поштучно или wipe + regenerate с бэкапом.

### Convention update: ARCHITECTURE.md §9 §10

Чтобы убрать NLP-эвристики на «блокирующий vs опциональный» Tech Debt — формат секций §9/§10 явно требует `[Priority]` тег:

```markdown
## 9. Tech Debt

- [High] <описание> — <почему блокер>
- [Medium] <описание> — <обоснование>
- [Low] <описание> — <почему не срочно>

## 10. Code Hotspots

- [High] <файл/модуль> — <чем проблемен>
- [Medium] ...
```

Уровни: **High / Medium / Low** (Critical опускаем — это уже P0, не Tech Debt).

**Правило промоушена в backlog:**

| Тег | Куда в backlog draft |
|---|---|
| `[High]` | `## Todo` как Tech Debt / Hotspot эпик |
| `[Medium]` | `## Todo` с пометкой «обсудить приоритет» |
| `[Low]` | `## Parking lot` |

**Затрагиваемые файлы (внутри будущего E-001 или отдельным эпиком):**

| Файл | Что меняется |
|---|---|
| `agents/architecture-interviewer.md` | Шаблон §9/§10 требует `[Priority]` префикс |
| `agents/docs-updater.md` | При добавлении Tech Debt / Hotspot пишет с `[Priority]` |
| `agents/backlog-planner.md` | Читает тег вместо текстовой эвристики (fallback на Medium для legacy записей) |

### Структура `.task/backlog.md` (полная)

```markdown
# Backlog — <project>

> **Edit log:**
> - 2026-04-26 · v0.3.0 · release-manager · E-001 → Done после релиза v0.3.0
> - 2026-04-22 · v0.3.0 · backlog-planner · sync — добавлен E-002 (новая Tech Debt запись)
> - 2026-04-15 · v0.2.1 · backlog-planner · создан

## In Progress

### E-001 [High] (Functional) Title
- Контекст: ...
- Зависит от: —
- Источник: OVERVIEW §6 Must

  - **E-001.1** [Critical] Done — Subtask
    - Ветка: `feat/E-001.1-name` · Волна 1 · Зависит от: —
  - **E-001.2** [High] In Progress — Subtask
    - Ветка: `feat/E-001.2-name` · Волна 2 · Зависит от: E-001.1
  - **E-001.3** [Medium] Failed — Subtask
    - Ветка: `feat/E-001.3-name` · Волна 2 · Зависит от: —

## Todo

### E-002 [Medium] (Tech Debt) Refactor X
...

## Done

### E-000 [Initial] v0.2.1 release
...

## Cancelled

_(пусто или эпики, отменённые через Abort)_

## Parking lot

- Test infrastructure — отложено YYYY-MM-DD

## Rejected suggestions

- CI setup — отклонено YYYY-MM-DD
```

---

## 9. `/plan` — детальный дизайн

### Цель

Тяжёлое интерактивное планирование одного эпика. На входе — эпик из `.task/backlog.md` (уже в `## In Progress` после `/backlog pick`). На выходе — `plan-E-001.md` (детальный артефакт) + обновлённый `.task/backlog.md` (подзадачи добавлены под уже-активным эпиком). Статус эпика `/plan` не меняет.

### Что такое подзадача

**Атомарная единица работы:**
1. Самостоятельна — execution-agent доделает без обращения к пользователю.
2. Мёрджится независимо — cascade-friendly: соседняя падает, эта остаётся валидной.
3. Чёткий DoD — известно, когда «сделано».
4. Один worktree → одна ветка `feat/E-001.X`.

**Не временной критерий** (не «1 час»), а **структурный** — атомарность, merge-independence.

### Типы подзадач

`impl` / `refactor` / `test` / `docs` / `research`.

**Тесты — отдельная подзадача / финальная волна / отдельный эпик** в зависимости от характера работы. **Не парные к каждой impl-подзадаче.** Изоляция от bias обеспечивается тем, что test-агент и impl-агент — разные.

| Кейс | Где тесты |
|---|---|
| Эпик вводит новую функциональность | Финальная волна — test-подзадача (одна или несколько) |
| Эпик — чистый рефакторинг | Существующие тесты, отдельная test-подзадача не нужна |
| Эпик — Tech Debt / Cleanup | На усмотрение `/plan` |
| Нет тестовой инфраструктуры | Отдельный эпик «Test infrastructure» |

### Фазы интерактивного диалога

| # | Фаза | Стиль |
|---|---|---|
| 1 | **Context** — read docs + light code scan, summary «что я понял» | Подтверждение |
| 2 | **Decomposition** — draft подзадач 5–15 шт. с типами и файлами | Bulk: split/merge/rename/reorder |
| 3 | **DAG + waves** — Mermaid граф, группировка в волны | Корректировки |
| 4 | **Risks + alternatives** — для развилок: альтернативы, +/−, рекомендация | One-question-at-a-time |
| 5 | **Open questions** — resolve / defer / promote to research subtask | По одному |
| 6 | **DoD** — acceptance-чеклист эпика | Bulk + правки |
| 7 | **Final review** — полный план, decision point | Accept / Iterate phase X / Abort |

**Если эпик мелкий** — фазы 4–5 пропускаются автоматически.
**Если эпик слишком большой** (>15 подзадач) — агент предупреждает, рекомендует разбить.

### Глубина light code analysis (фаза 1)

| Слой | Что | Глубина |
|---|---|---|
| 1. Docs | OVERVIEW, ARCHITECTURE, CLAUDE, backlog | Полностью |
| 2. Project skeleton | `ls -R`, `git log --oneline -20`, `.gitignore` | Полностью |
| 3. Likely affected modules | По keywords из title + «Источник» эпика + упоминаниям в docs | Полностью |
| 4. Adjacent modules | Соседние / зависимые | Заголовки (имена функций/классов через grep) |
| 5. Остальное | — | Не читает |

**Транспарентность:** в конце фазы 1 показывает «Я сфокусировался на [files]. Добавить?» — у пользователя один шанс расширить read-list **до** декомпозиции.

**Адаптивность:** если в фазе 2 нужно прочитать ещё файл — читает на лету.

### Поведение в фазе 4 при «выбери сам»

| Уверенность агента | Поведение |
|---|---|
| Сильная рекомендация | Принимает сам, помечает в плане **«delegated to agent»**, кратко обосновывает |
| Слабая рекомендация | Push back один раз: «развилка влияет на [X], мой lean — Y из-за [причина]. Точно делегируешь?» |
| 50/50 | Push back более настойчиво. Если всё равно «решай сам» → берёт lean, помечает **«weak choice — re-evaluate before /execute»** |

Все agent-делегированные решения попадают в `plan-E-001.md` § «Альтернативы» с явной пометкой и пунктом **Re-visit if:**.

### Цикл итераций

**Phase 7 — единственная decision point** для кросс-фазных переходов:

| Действие | Что происходит |
|---|---|
| Accept | `plan-E-001.md` пишется, в `backlog.md` под активным эпиком добавляются подзадачи. Статус эпика не меняется (он уже In Progress) |
| Iterate phase X | Прыгает в X, переоткрывает, **re-flows** downstream фазы (показывает дельты, не весь поток) |
| Abort | Ничего не пишется, статус эпика не меняется (остаётся In Progress без плана — пользователь может перезапустить `/plan` или вернуть в Todo через `/backlog` action `unselect active epic`) |

Внутри фазы — свободная навигация (split/merge подзадач). Между фазами — только через phase 7.

**Soft cap:** третья итерация той же фазы → агент сигналит «возможно эпик слишком большой / нечётко определён», предлагает разбить или доуточнить docs.

### Research subtask — контракт

**Когда:**
- Открытый вопрос, требующий исследования перед impl
- Развилка без явной рекомендации (фаза 4 не зарезолвила)
- Spike / выбор библиотеки / прототип

**DoD:** файл `.task/research-E-001.X.md` с фиксированной структурой:

```markdown
# Research E-001.X: <вопрос>

## Question
## Options (с +/−)
## Decision
## Rationale
## Implications (что это значит для зависимых impl-подзадач)
```

**Worktree и ветка:** как у impl. В коммите — research-файл (+ опционально код прототипа).

**Интеграция:** мёрджится в `feat/E-001`, downstream impl-подзадачи читают из integration ветки.

**Failure:** если research-агент не пришёл к decision → подзадача `Failed`, cascade на зависимых.

**Bias:** тот же агент потом не делает impl своей research'нной фичи (research-агент → impl-агент в следующей волне читает решение и делает).

### Формат `.task/plan-E-001.md`

```markdown
# Plan — E-001 [High] (Functional) <Title>

Дата: YYYY-MM-DD · Статус: Final

## 1. Контекст

## 2. Подзадачи

### E-001.X [Priority] (type) <Title>
- Что делает: ...
- Файлы: ...
- DoD: ...
- Ветка · Волна · Зависит от

## 3. DAG (Mermaid)

## 4. Волны

## 5. Риски (с mitigation)

## 6. Альтернативы (рассмотрены и отклонены, включая «delegated to agent»)

## 7. Открытые вопросы (resolved / deferred / research subtask)

## 8. DoD эпика (чеклист условий для /release)
```

**Источник истины:** plan = детальный контекст подзадач, backlog.md = state at a glance (компактные строки).

---

## 10. `/execute` — детальный дизайн

### Концепция

«Автономно от старта до финала, диалог только в начале и в конце». Pre-execute confirmation (один раз) → автономный прогон всех волн → агрегированный отчёт + диалог.

### Команда

```
/execute
```

**Без аргументов и флагов.** Активный эпик определяется автоматически из секции `## In Progress` в `.task/backlog.md`. Нет `--dry-run`, `--no-confirm`, `--resume-merges` и т.п. Доверяем агенту на этапе планирования и исполнения, аудит — в финальном диалоге.

### Orchestration mechanism

**Task tool subagents.** Главный агент (handler `/execute`) спаунит execution-agent'ов параллельно через несколько Task-вызовов в одном сообщении. Release-manager — тоже subagent.

- Subagents имеют независимый conversation context
- Файловая система общая, но каждый работает в своём worktree
- Иерархия: orchestrator → execution-agent (без вложенности; subagents не могут спаунить других)

### Context control (4 слоя)

1. **Стандартный** (auto, фиксированный): CLAUDE.md, путь к plan-E-001.md, спецификация подзадачи, путь worktree, имя ветки.
2. **Декларируемый** (control через `/plan`): поле «Контекст» в каждой подзадаче `plan-E-001.md` — список доп. файлов для чтения.
3. **Pre-execute summary** (один раз перед стартом): orchestrator печатает, что получит каждый subagent. Confirm `[y/a]`.
4. **Без `--dry-run`** — отказались, аудит только постфактум.

**Изоляция:** subagents не видят друг друга, conversation-history, чужие worktree.

### Execution-agent contract

#### Authority

| Может | Не может |
|---|---|
| Читать любые файлы в репо | Спаунить других subagent'ов |
| Писать / править в **своём** worktree | Пушить на remote (`git push`) |
| Запускать bash (git local, build, lint, test) | Модифицировать main или чужие worktree |
| `git worktree add`, commit на своей ветке | Спрашивать пользователя в реалтайме |
| Писать в `.task/report-E-001.X.md` | Использовать веб-инструменты |
| | Менять `.task/backlog.md`, `plan-E-001.md` |

#### Жизненный цикл

```
1. Receive prompt от orchestrator (Task tool)
2. git worktree add <path> -b feat/E-001.X feat/E-001
3. cd <worktree>
4. Read plan-E-001.md + свой subtask spec + декларированный контекст
5. Implement: edit / write / bash для тестов и сборки
6. Commit(s) на feat/E-001.X
7. Smoke check (по DoD)
8. Write .task/report-E-001.X.md
9. Return summary orchestrator'у
```

#### Возврат orchestrator'у (короткий summary)

```
Status: Done | Failed | Partial
Subtask: E-001.X
Branch: feat/E-001.X
Commits: 3
  - sha1, sha2, sha3
Smoke check: pass | fail | n/a
Note: <одна строка> (опционально)
```

Полный контекст — в файле отчёта. Возврат = карта для orchestrator'а.

#### Обстукло

**Когда:** файл из спеки не существует; smoke check fail после одной попытки; spec противоречит сама себе; развилка требует решения уровня пользователя; bash падает с непонятной ошибкой.

**Что делает:**
1. **Не делает rollback** — worktree и partial commits сохраняются для инспекции
2. Пишет секцию **Obstacles** в отчёт (tried / stopped at / need)
3. Возвращает **Status=Failed**
4. Cascade — на orchestrator'е (DAG из плана)

**Не пытается** обойти спеку, не зацикливается на retry, не возвращает success при фундаментальном фейле. Bash hang >10 минут → прерывает.

#### Лимиты

Один execution-agent = одна Task tool сессия = одна subtask.

Если context exhausted — Failed, obstacle = «context exhausted, recommend split». Превентивных эвристик **нет**, реактивный fallback в финальном диалоге.

### Smoke check

**Гибрид:** явная команда из DoD приоритетна, иначе type-based default.

| Тип | Default smoke check |
|---|---|
| `impl` | Тесты для затронутых файлов + lint + build |
| `refactor` | Существующие тесты зелёные |
| `test` | Тесты, которые написала эта подзадача, проходят против `feat/E-001` |
| `docs` | Markdown lint / link check, иначе файл существует |
| `research` | Research-файл существует и содержит 5 секций |

**Поведение при fail:**
- Одна попытка авто-фикса
- Не помогло → Status=Failed, obstacle с command/exit/output/tried/stopped at
- Не пытается отключать тесты ради «прохождения»

### Worktree management

#### Pre-execute analysis (когда есть стейт от прерванного прогона)

Anti-data-loss: orchestrator не чистит автоматически. Анализ → диалог → адаптация плана.

| Состояние | Default action |
|---|---|
| Worktree + commits + report Status=Done + smoke OK | Skip, mark Done в текущем плане |
| Worktree + commits + report Status=Partial | Resume / Restart |
| Worktree + commits + НЕТ отчёта | Resume / Restart |
| Worktree + commits + report Status=Failed | Restart / re-open /plan / Skip |
| Только ветка без worktree | Recreate worktree (resume) |
| Worktree без коммитов | Clean safe |
| **Uncommitted changes** | **Hard stop** — ручной разбор |

**Resumed подзадача** получает в prompt'е секцию «Resume context» с описанием уже сделанного.

#### Cleanup worktree'ев — все transitions

| Transition | Что с worktree |
|---|---|
| Subtask `Done` + merge OK (release-manager merge mode) | Worktree удаляется + branch удаляется |
| Subtask `Done` + `Conflict-blocked` (high-stakes) | Worktree сохраняется до резолва через финальный диалог |
| Subtask `Failed` (от execution-agent) | Worktree сохраняется для инспекции пользователем |
| Subtask `Skipped` (cascade) | Worktree не создавался |
| **`Skip permanently`** в финальном диалоге | Подзадача → `Cancelled`, **worktree удаляется** |
| **`Abort epic`** в финальном диалоге | **Все worktree'ы эпика удаляются + feat/E-001 удаляется**, эпик → `Cancelled` |
| **`/plan iterate`** меняет состав подзадач | Worktree'ы удалённых / переразбитых подзадач **очищаются до записи нового плана** |

Если все эти переходы корректны — `.worktrees/` не разрастается, отдельный cleanup-механизм не нужен (см. §13.8).

#### Conflicts

**Три уровня резолва (детали в §13.5):**

1. **Combine (default)** — пробует объединить дополняющие изменения (разные импорты, разные методы, разные секции). Большинство конфликтов так. Логирует в Decisions агрегата.
2. **Auto-pick (low-stakes contradiction)** — combine невозможен И не high-stakes. Выбирает по intent analysis, логирует с обоснованием в Decisions.
3. **User dialog (high-stakes contradiction)** — combine невозможен И high-stakes (public API breaking / security / data integrity / critical config). Подзадача → `Conflict-blocked`. Cascade на зависимых.

Auto-resolved (combine + auto-pick) попадают в **Decisions** агрегата с возможностью Revert. High-stakes → секция Conflicts в финальном меню с интерактивным выбором версии.

### Per-subtask report — `.task/report-E-001.X.md`

Пять **обязательных** секций (даже если пустые `_none_`):

```markdown
# Report — E-001.X

**Status:** Done | Failed | Partial
**Subtask:** E-001.X — Title
**Type:** impl
**Started:** ISO timestamp
**Finished:** ISO timestamp
**Worktree:** .worktrees/E-001.X
**Branch:** feat/E-001.X
**Commits:** [list]

## Done
<bullet list, что реализовано + результат smoke check>

## Decisions
<автономные выборы: AD-X с Choice / Why / Re-visit if>

## Obstacles
<пусто для Done; для Failed/Partial — tried / stopped at / need>

## Open questions
<замечания, требующие решения уровнем выше>

## Follow-ups
<кандидаты на новые подзадачи: тип + краткое описание>
```

### Aggregated report — `.task/report-E-001.md`

Пишет release-manager в aggregation-режиме, читая все per-subtask отчёты + свои merge-findings + результат code-reviewer'а.

**Секции в порядке важности:**

1. Summary (status, stats)
2. Wave breakdown
3. **Obstacles** (failed subtasks с tried/stopped at/need)
4. **Conflicts** (high-stakes only, awaiting user resolution; auto-resolved combine + auto-pick попадают в Decisions)
5. **Review** (от code-reviewer, severity Critical/High/Medium/Low)
6. Open questions (агрегат)
7. Decisions (агрегат)
8. Follow-ups suggested
9. Integration state (что в feat/E-001, kept worktrees/branches)

### Code-reviewer pass перед финальным диалогом

**Внутренний агент**, тот же `code-reviewer`, в **mode `in-execute`** (фокус code quality после интеграции волн). Запускается всегда, если эпик не failed целиком. Mode `release-readiness` отрабатывает позже, в `/release` step 2.

**Читает:** feat/E-001 после всех merge'ей (полный diff vs main), aggregated report, plan-E-001.md, CLAUDE.md.

**Ищет:**
- Cross-subtask issues (дублирование, дрифт стилей)
- Integration concerns (от сочетания merge'ей)
- Quality на epic-level (тесты, документация, naming)
- Соответствие плану

**Не делает:** не переписывает код, не запускает тесты, не блокирует.

**Findings** нумеруются `R-X`, попадают в секцию Review агрегированного отчёта.

### Inter-wave integration

```
Wave W:
  1. Spawn N execution-agent через Task tool параллельно
  2. Wait all returns
  3. Spawn release-manager (merge mode) для волны W
  4. Merge-loop: для каждой Done — git merge --no-ff в feat/E-001
     - Success: cleanup worktree + delete branch
     - Conflict: попытка combine → auto-pick (low-stakes) → high-stakes
       → подзадача Conflict-blocked, worktree сохраняется. См. §13.5
     - Failed/Partial: не мёрджим
  5. Optional post-wave smoke check на feat/E-001
  6. Cascade: orchestrator вычисляет downstream Skipped по DAG
  7. → Wave W+1
```

**Никакой интерактивности между волнами.** Один pre-execute confirm + автономия до финального диалога.

### Final dialog

#### Step 1 — narrative summary (структура + живой тон)

Скелет фиксированный, тон conversational:

```markdown
Epic E-001 — отчёт

## Сделано
<connected текст что закрыто, что в feat/E-001>

## Не сделано
<failed / skipped / blocked, конкретно>

## Что это значит
<implication для проекта: что не работает, чего не хватает>

## Требуются действия
<главная развилка с рекомендацией и обоснованием>

## Иначе
<последствия каждого пути: accept partial / defer>

## Дополнительно
<обзор decisions / open questions / follow-ups / review findings>

## Что обсуждаем?
<меню>
```

Длина — по содержанию, без искусственных лимитов.

#### Step 2 — финальное меню (фиксированный порядок по убыванию важности)

```
1. Obstacles         (X failed + Y skipped)
2. Conflicts         (N high-stakes blocked)
3. Review findings   (Critical / High / Medium / Low)
4. Open questions    (N)
5. Decisions         (M)
6. Follow-ups        (K)
7. Show full report
8. Skip, go to final action
```

Стрелка `← рекомендую начать здесь` ставится туда, где реально требует внимания: Obstacles → Conflicts → Review Crit/High → Open → ничего.

#### Step 3 — sub-dialogs по выбранным темам

Каждая категория имеет свои действия:

- **Obstacles:** Re-plan / Replace DoD and re-run / Skip permanently / Manual intervention
- **Review (Critical/High):** Fix now (mini-execution-agent) / Acknowledge as known limitation / Abort release
- **Review (Medium/Low):** Accept / Fix / Defer
- **Open questions:** Resolve now / Defer / Promote to research subtask
- **Decisions:** Accept / Mark for re-visit / Revert (новая subtask)
- **Conflicts (high-stakes only):** Take version 1 / Take version 2 / Combine (опиши как) / Skip merge (→ Cancelled) / Show full diff
- **Follow-ups:** Accept (→ backlog) / Reject

#### Step 4 — финальное действие

```
[r] /release         — текущее состояние feat/E-001 готово к релизу
[i] Iterate                — re-plan failed/blocked, /plan phase 2
[p] Partial /execute       — перезапустить только failed/skipped без re-plan
[d] Defer                  — оставить эпик в In Progress, выйти
[a] Abort epic             — откатить feat/E-001, эпик → Cancelled
```

**Нельзя выйти без явного финального действия** — orchestrator переспрашивает.

---

## 11. `/release` — детальный дизайн

### Концепция

**Агент готовит материалы, не релизит сам.** `/release` пишет файлы локально, коммитит на `feat/E-001`, печатает инструкции пользователю. **Не делает** `git merge → main`, `git tag`, `git push`. Это руки пользователя.

### Команда

```
/release
```

**Без аргументов и флагов.** Активный эпик определяется автоматически из секции `## In Progress` в `.task/backlog.md`.

### Высокоуровневый flow

```
1. Verify preconditions
   - Эпик в In Progress, feat/E-001 существует, есть aggregated report
2. Independent pre-release review
3. Determine epic state (Success / Partial / Failed / Conflicts)
4. Suggest version bump
5. Prepare release materials (drafts по одному)
6. Commit на feat/E-001 ("release: prepare vX.Y.Z")
7. Update backlog (E-001 → Done с датой)
8. Print user instructions (НЕ ВЫПОЛНЯЕТ)
```

### Чёткое разделение ответственности

| Что | Агент | Пользователь |
|---|---|---|
| Independent review feat/E-001 | ✅ | — |
| Update CHANGELOG / version files | ✅ | проверяет, корректирует |
| Commit на feat/E-001 | ✅ | — |
| Update backlog (Done) | ✅ | — |
| `git checkout main && merge` | — | ✅ |
| `git tag` | — | ✅ |
| `git push && git push --tags` | — | ✅ |
| GitHub Release | — | ✅ |

### Independent pre-release review

**Тот же `code-reviewer` agent**, но с release-readiness промптом — другой фокус, чем review в `/execute`.

| Проверяется | Не проверяется (это сделал review в /execute) |
|---|---|
| Public API / surface changes — задокументированы? | Cross-subtask code duplication |
| Breaking changes — пометка в CHANGELOG? | Integration concerns |
| Migration steps — достаточно? | Code style |
| README отражает новые команды? | |
| ARCHITECTURE.md / OVERVIEW.md обновлены? | |
| Hooks обновлены под новый формат? | |
| Версии в plugin.json / marketplace.json consistent? | |

**Findings:** `PR-X` (Pre-Release-1, ...). Severity: Critical / High / Medium / Low.

**Действия:**
- **Critical / High блокируют release** — Fix now (mini-execution-agent для тривиальных правок) / Acknowledge as known limitation (внести в CHANGELOG секцию Known Limitations) / Abort release
- **Medium / Low** — Accept / Fix / Defer

### Partial release policy

| Status в aggregated report | Поведение `/release` |
|---|---|
| `Success` | Прямой путь, без доп. подтверждений |
| `Partial` | Подтверждение «релизим как есть» + auto-секция Known Limitations в CHANGELOG из failed/skipped |
| `Failed` | Отказ работать (нечего релизить) |
| `Unresolved conflicts` | Отказ работать, пока конфликты не закрыты |

### Version bump heuristic

**Агент предлагает с обоснованием, пользователь подтверждает.**

| Изменения | Pre-1.0 (0.x.y) | 1.0+ |
|---|---|---|
| Только bug fixes / docs / refactor | patch | patch |
| Новые фичи / non-breaking | minor | minor |
| Breaking changes | minor | major |

В pre-1.0 любые breaking → minor по convention (Cargo / npm / etc).

При смешанных изменениях агент показывает все triggers и lean.

### Release artifacts (набор)

| Артефакт | Файл | Аудитория |
|---|---|---|
| **CHANGELOG entry** | `CHANGELOG.md` (append) | Технический, Keep a Changelog |
| **Release notes** | `.task/release-notes-vX.Y.Z.md` | Для GitHub Release — пользовательский язык, highlight'ы, install/upgrade |
| **Announcement** | `.task/announcement-vX.Y.Z.md` | Для TG / блога / social — короткий, цепляющий |
| **Tag message** | (текст, не файл) | 2–4 строки для `git tag -a` |
| **Migration guide** | `.task/migration-vX.Y.Z.md` (только при breaking) | Шаги обновления |

**Источники для генерации:** aggregated report, `git diff main..feat/E-001`, plan-E-001.md, existing CHANGELOG (для тона).

**Диалог по артефактам — пошаговый, не одним полотном.** По каждому: accept / edit / regenerate.

### Multi-epic release

**Структурно невозможно** из-за «один эпик In Progress за раз»: пока `/release` не закроет E-001, нельзя начать E-002. Каждый эпик → один релиз.

### Rollback policy

**Точки abort'а внутри `/release`:**

| До шага 6 (commit) | Просто выход, ничего откатывать |
|---|---|
| После шага 6/7 | Нужны действия — `/release` НЕ делает auto-undo, печатает точные инструкции пользователю |
| После пользовательского merge на main / tag | За пределами `/release`, стандартный git |

**Edge case:** запуск `/release` на эпике с уже подготовленным release commit — pre-execute analysis детектит, предлагает Continue / Rollback / Abort.

### Release-manager modes (агент с тремя режимами)

| Режим | Где | Что делает |
|---|---|---|
| **merge** | `/execute` между волнами | Merge subtask-веток в `feat/E-001`, cleanup worktree'ев Done подзадач, conflict report с proposals |
| **aggregation** | `/execute` после всех волн | Собирает `.task/report-E-001.md` из per-subtask отчётов + merge findings + (после code-review pass) review findings |
| **full** | `/release` | Independent pre-release review (через code-reviewer), generate материалов, commit на feat/E-001, update backlog, print instructions |

---

## 12. Инфраструктура

### 12.1. Hooks

В `hooks/hooks.json` остаётся **один** SessionStart hook, переписанный под двухуровневую модель.

**Что выводит:**

```
dm-cc-assistant v0.3 loaded.
- Backlog: 1 in progress, 4 todo, 2 done
- Active: E-001 «Двухуровневый бэклог» (3/7 done, wave 2/3)
- .task/plan-E-001.md ready (если есть)
- .task/report-E-001.md ready — review needed (если есть)
Commands: :backlog | :plan | :execute | :release
```

**Bash-логика:** парсит `.task/backlog.md` через awk/grep:
- Подсчёт эпиков по секциям `## In Progress` / `## Todo` / `## Done` через `^### E-` строки
- Active epic = первый эпик в `## In Progress`, извлекаем title
- Прогресс активного: подсчёт подзадач `^  - \*\*E-XXX\.\d+\*\*` по статусам inline
- Wave: из поля «Волна N» в строке подзадачи
- Plan / report ready: проверка существования `.task/plan-E-*.md` / `.task/report-E-*.md`

**Edge cases:** пустой backlog → подсказка `/backlog`. Нет CLAUDE.md → подсказка `/project-init`. Backlog в формате v0.2 → подсказка миграции.

**Других хуков не добавляем.** Task tool subagents возвращают результаты сами, дополнительной сигнализации не нужно.

### 12.2. Миграция v0.2 → v0.3

Обновление плагина не трогает проектное состояние. Миграция запускается автоматически в `/backlog` при детекте старого формата.

**Детект формата:**
- Первая строка типа `# Backlog — ...` И есть `^- \*\*T-\d+` строки → v0.2 flat
- Есть `^### E-\d+` → v0.3 epics
- Edit log с `dm-cc-assistant vX.Y.Z` — определяет точную предыдущую версию

**Migration options в `/backlog`:**

| Опция | Что делает |
|---|---|
| **Migrate (default)** | Done T-IDs группируются в эпик `E-000 «vX.Y release»`. Active T-IDs становятся подзадачами выбранного эпика или новых. Backlog переписывается в v0.3 формате |
| **Wipe** | Бэкап в `.task/backlog.v02.md`, запускается обычный first-run flow (генерация эпиков из docs) |
| **Keep-legacy** | T-IDs остаются в секции `## Legacy`, новые эпики — в стандартных секциях. Двойная модель, для постепенного перехода |
| **Abort** | Не запускать `/backlog` в v0.3, оставить как есть |

**Старые `.task/` файлы** (`research.md`, `review.md`, `release-notes-v0.2.x.md`, `tg-post.md`) — не трогаем, остаются как исторические артефакты.

**Удалённые команды** (`/research`, `/review`, `/update-docs`) — Claude Code вернёт «skill not found» при попытке вызова. В CHANGELOG явная пометка `### Removed (Breaking)`, в README — migration note, в `.task/migration-v0.3.0.md` — пользовательская инструкция (генерируется в `/release`).

### 12.3. Edit log convention

Все генерируемые файлы получают edit log сразу после заголовка:

```markdown
# OVERVIEW.md — <Project>

> **Edit log:**
> - 2026-04-26 · v0.3.0 · docs-updater · обновил §2 Module Map
> - 2026-04-18 · v0.2.0 · docs-updater · v2-фичи перенесены в Must
> - 2026-04-15 · v0.1.0 · overview-interviewer · создан

## 1. Обзор и цель
...
```

**Формат строки:** `- YYYY-MM-DD · vX.Y.Z · <agent-name> · <краткое описание правки>`

**Latest first** — новое сверху.

**Применяется к:**
- Doc files: `OVERVIEW.md`, `ARCHITECTURE.md`, `CLAUDE.md`
- State files: `.task/backlog.md`, `.task/plan-E-XXX.md`, `.task/report-E-XXX.X.md`, `.task/report-E-XXX.md`
- Drafts: `.task/research-E-XXX.X.md`, `.task/release-notes-vX.Y.Z.md`, `.task/announcement-vX.Y.Z.md`, `.task/migration-vX.Y.Z.md`

**Не применяется к:**
- `README.md`, `LICENSE`, `CHANGELOG.md` — пользовательские, не генерируемые целиком
- `.claude/` scaffold — пользовательский после `/project-init`

**Поведение агентов:** при записи / обновлении файла читают существующий лог (если есть), добавляют новую строку сверху, записывают. Версия плагина и имя агента — захардкожены в промпте каждого агента. Дата — текущая.

**Migration legacy файлов:** при первой правке файла без edit log агент создаёт секцию с одной записью (текущая правка). История до этого момента не восстанавливается.

### 12.4. Изменения в файлах под v0.3

#### Добавляется

| Файл | Назначение |
|---|---|
| `agents/epic-planner.md` | Тяжёлое планирование одного эпика |
| `agents/execution-agent.md` | Автономная работа в worktree |
| `agents/resume-analyzer.md` | Анализ прерванной подзадачи для resume context'а |
| `skills/plan/SKILL.md` | Оркестратор `/plan` |
| `skills/execute/SKILL.md` | Оркестратор `/execute` |

#### Меняется

| Файл | Изменения |
|---|---|
| `agents/backlog-planner.md` | Двухуровневая модель, sync, suggestions с rejected memory, миграция, validate-step при add-new-epic |
| `agents/release-manager.md` | Три режима: `merge` / `aggregation` / `full`. Полностью новая логика |
| `agents/code-reviewer.md` | Расширить до двух mode: `in-execute` (был, code quality после волн) + `release-readiness` (новый, shipping readiness в /release) |
| `agents/architecture-interviewer.md` | Шаблон §9/§10 требует `[Priority]` префикс |
| `agents/docs-updater.md` | При записи Tech Debt / Hotspot пишет `[Priority]` |
| `hooks/hooks.json` | SessionStart считает эпики, новый набор команд |
| `skills/backlog/SKILL.md` | Под двухуровневую модель |
| `skills/release/SKILL.md` | Под новый flow без push |
| `.claude-plugin/plugin.json` | `version: "0.3.0"`, обновлённый description, keywords |
| `.claude-plugin/marketplace.json` | sync с plugin.json |
| `README.md` | Раздел «Что умеет», «Структура плагина», «Типичный цикл», migration note |
| `OVERVIEW.md` | §4, §5, §6, §7, §8, §9 — обновления под v0.3 модель |
| `ARCHITECTURE.md` | §1, §2, §3, §5, §7, §8, §9, §10 — большие правки |
| `CLAUDE.md` | WHAT, HOW, structure, CONSTRAINTS |
| `CHANGELOG.md` | Entry `[0.3.0]` |

#### Удаляется

| Файл / директория | Почему |
|---|---|
| `skills/research/` | `/research T-ID` снаружи больше нет |
| `skills/review/` | `/review` снаружи больше нет |
| `skills/update-docs/` | `/update-docs` снаружи больше нет |
| `agents/task-researcher.md` | Работа поглощена `/plan` |

### 12.5. Версионирование v0.3.0

**Bump:** `0.2.1 → 0.3.0` (minor). Содержит breaking changes (удалённые команды, изменение формата backlog), но в pre-1.0 проектах breaking → minor по convention.

**`plugin.json`:**

```json
{
  "version": "0.3.0",
  "description": "Full development lifecycle assistant: project initialization
    (OVERVIEW, ARCHITECTURE, CLAUDE.md), epic-based backlog planning,
    autonomous parallel execution with worktree isolation, and release
    preparation.",
  "keywords": [
    "project-init",
    "backlog",
    "epic",
    "autonomous",
    "parallel",
    "worktree",
    "release",
    "scaffolding",
    "documentation",
    "kmp"
  ]
}
```

**`marketplace.json`:** sync version и description.

**Tag:** `v0.3.0`, annotated message — release-manager сгенерирует short summary.

**CHANGELOG entry [0.3.0]** — структура:
- `### Added` — 4 новые команды снаружи (хотя `/backlog` была, но переосмыслена), новые агенты, режимы release-manager, edit log, `[Priority]`, sync, suggestions
- `### Changed` — backlog-planner, release-manager, code-reviewer, architecture-interviewer, docs-updater, hooks
- `### Removed (Breaking)` — `/research`, `/review`, `/update-docs`, task-researcher, плоский T-ID формат
- `### Migration` — ссылка на `.task/migration-v0.3.0.md`, описание автомиграции backlog

### 12.6. Курица-яйцо релиза

Сам v0.3.0 будет зарелизен с использованием **v0.2.1 release-skill** (потому что v0.3 ещё нет на момент его выпуска):
- `/dm-cc-assistant:release full` подготовит CHANGELOG, version files, tag message по старому процессу
- Дальнейшие релизы будут уже через v0.3 flow (`/release`)

---

## 13. Smoke check resolutions

После основного дизайна прошлись по 10 дырам на стыках разделов. Резолвы здесь — корректируют / дополняют ранее описанное.

### #1. Транзишен Todo → In Progress без плана

Эпик в `## In Progress` без подзадач — валидное состояние «активный, не спланирован».
- SessionStart показывает: «E-001 (no plan yet) — run /plan»
- `/execute` отказывается, направляет к `/plan`
- В меню `/backlog` добавляется action **«unselect active epic»** — переводит обратно в Todo, ничего не теряем

### #2. Research subtask пишет оба файла

Research-подзадача создаёт **два файла**:
- `.task/research-E-XXX.X.md` — deliverable (вопрос/опции/решение/обоснование/impacts), мёрджится в `feat/E-001`
- `.task/report-E-XXX.X.md` — стандартная 5-секционная отчётность как у любой подзадачи

Логика: любая подзадача пишет per-subtask report (обязательная отчётность). Research-подзадача дополнительно пишет research-файл (свой deliverable, как код для impl-подзадачи).

### #3. Cascade — бинарное правило

Если **любая** dependency подзадачи не в `Done` (Failed / Skipped / Conflict-blocked) — подзадача `Skipped` с reason `blocked by E-001.X`.

Никакой логики «частичное покрытие» — это перекладывает ответственность и плохо семантически. Пользователь в финальном диалоге сам решает, нужно ли частичное переисполнение через iterate.

### #4. Resume context — файл

`.task/resume-E-XXX.X.md` создаётся `resume-analyzer` агентом (см. #9) при выборе пользователем Resume в pre-execute analysis. Resumed execution-agent читает его перед стартом.

Структура:
```markdown
# Resume context — E-001.X

> **dm-cc-assistant v0.3.0** / resume-analyzer · YYYY-MM-DD

## Что сделано в прошлом запуске
<connected analysis от resume-analyzer>

## Что осталось до DoD
- TODO 1, TODO 2, ...

## Что осторожно сохранять
<рискованные части — recommend rewrite>

## Decisions из прошлого отчёта
- AD-1: ... — Status: still valid | reconsider

## Контекст исполнения
- Ветка / коммиты / файлы / timestamp / путь к прошлому report

## Recommendation
- Confidence: high | medium | low
- Action: Resume safe | Restart recommended | Resume with caveats
```

Файл живёт в `.task/`, не gitignor'ится, может попасть в коммит. Перезаписывается при следующем Resume той же подзадачи.

### #5. Conflict resolution — три уровня

Заменяет описание «не пытается резолвить + только описывает proposal» из section 10.

**Prevention в `/plan` phase 3 (DAG + waves):** для каждой пары подзадач в одной волне — пересечение поля `Файлы:`. Overlap → разные волны или слияние в одну подзадачу. Это структурная инвариантa, не эвристика.

**Resolution в release-manager (merge mode) — три уровня:**

| Уровень | Когда срабатывает | Действие |
|---|---|---|
| **1. Combine** | Default, любой конфликт сначала | Объединяет дополняющие изменения (разные импорты, разные методы, разные секции). Большинство конфликтов резолвится так. Логирует в Decisions агрегата |
| **2. Auto-pick** | Combine невозможен + low-stakes | Выбирает версию по intent analysis (соответствие плану / общность / dependency chain). Логирует с обоснованием в Decisions |
| **3. User dialog** | Combine невозможен + high-stakes | Подзадача → `Conflict-blocked`. Cascade. В финальном диалоге `/execute` категория Conflicts с выбором: take version 1 / take version 2 / combine (опиши как) / skip merge |

**High-stakes категории:**
- Public API breaking (изменение сигнатуры, удаление публичной функции, изменение формата файла)
- Security (auth, encryption, permissions, secrets)
- Data integrity (миграции, удаление данных, изменение data model)
- Critical config (production credentials, deployment, CI/CD)

Агент детектит high-stakes по паттернам файлов (можно явно перечислить в проектном CLAUDE.md) + природе изменения (удаление, breaking signature change).

**Auto-resolved попадают в Decisions** агрегированного отчёта с возможностью Revert.

**В финальном меню** категория Conflicts чаще всего пустая (только high-stakes). Меню по conflict'у не предлагает manual resolve — только варианты выбора через диалог.

### #6. Backlog edit во время `/execute`

В backlog пишут только **последовательные** агенты (orchestrator, release-manager между волнами). Execution-agent'ы не пишут — inter-agent race нет.

**Race с пользователем — soft signal без жёсткого lock'а:**
- `.task/.execute-active` marker file создаётся orchestrator'ом при старте, удаляется при завершении
- SessionStart hook при наличии файла предупреждает «не редактируй backlog руками»
- Release-manager перед записью перечитывает backlog, делает diff с тем что было, при detected external change — пишет своё, в отчёт note

Convention документируется. Hard lock не делаем.

### #7. Guided recovery вместо отказов

**Заменяет** правило «pre-condition не выполнен → отказ» на **recovery-диалог с конкретными вариантами + опциональным chain'ом**.

Каждая команда при детекте inconsistency не отказывается, а:
1. Предлагает варианты восстановления (specific to context)
2. При выборе пользователя — может **chain'ом** запустить другую команду

Примеры:
- `/plan` без активного эпика → «Открыть Todo и выбрать?» → запуск branch'а `/backlog` (pick from Todo)
- `/execute` без plan'а → «Запустить /plan?» → переход в /plan, после accept'а возврат к /execute
- `/release` без report'а → «Запустить /execute?» → переход, после возврата — продолжение /release
- Plan ID mismatch → «Переключить активный эпик / создать новый план / удалить orphan?»

`/backlog` делает активную детекцию: при каждом запуске сканирует `.task/plan-E-*.md` и `.task/report-E-*.md`, surface'ит inconsistency'и в обзоре с действиями.

**Auto-recovery без диалога** — только при безопасных кейсах в `[Cleanup orphans]` action: orphan от Cancelled / not-found эпиков. Странные случаи (Done без plan) — инфо, без auto-удаления.

### #8. Worktree cleanup через state transitions

Не нужен отдельный cleanup-механизм. Каждый end state должен корректно очищать worktree.

**Дополнения к state transitions (требования):**
- **«Skip permanently»** в финальном диалоге `/execute` → подзадача `Cancelled` + worktree удаляется
- **«Abort epic»** в финальном диалоге → удаляются ВСЕ worktree'ы эпика + feat/E-001
- **`/plan` iterate (phase 7)** меняет состав подзадач (удалена / переразбита) → cleanup worktree'ев изменённых подзадач до записи нового плана

Если все эти переходы корректны — `.worktrees/` не разрастается. Никаких отдельных tools'ов и stale-детекторов.

### #9. Code-reviewer + resume-analyzer (вариант C)

**`agents/code-reviewer.md`** — два mode:
- `in-execute` review (cross-subtask quality после волн)
- `release-readiness` review (pre-release в /release, фокус shipping)

**`agents/resume-analyzer.md`** — отдельный агент:
- Анализ прерванной подзадачи для построения resume context'а
- Не severity-based finding'и, а структурированный план продолжения

Логика: in-execute и release-readiness — оба смотрят diff → severity findings, паттерн один. Resume-analysis — другая работа, выводит actionable recovery plan.

**Изменения в section 3 (агенты):**

| Агент | Статус |
|---|---|
| `code-reviewer` | Расширить — два mode (in-execute / release-readiness) |
| `resume-analyzer` | **Новый** — анализ прерванной подзадачи |

### #10. Auto-archive в `/release`

После step «Update backlog (E-XXX → Done)»:
1. Подсчитать эпики в `## Done`
2. Если **больше 5** — переместить самые старые в `.task/backlog-archive.md`

Archive append-only, latest first. backlog-planner при генерации новых ID читает оба (`backlog.md` + `backlog-archive.md`) для уникальности.

Захардкоженный лимит 5. Без диалога, без действий пользователя. Возврат эпика из архива — ручной (копирование секции).
